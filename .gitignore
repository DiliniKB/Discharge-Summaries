# Dev-only database and attachments — never commit real or fixture patient data
data/

# Python
__pycache__/
*.pyc
.venv/

# PyInstaller build output — NOT build.spec itself: that's a committed,
# hand-authored source file (excludes/datas/icon config), not a
# PyInstaller-generated artifact. `*.spec` here would silently untrack it.
build/
dist/

# macOS
.DS_Store
